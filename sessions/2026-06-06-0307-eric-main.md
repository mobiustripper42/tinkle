---
session: 4
dev: eric
slug: main
branch: main
started: 2026-06-06T03:07:11Z
ended:
points:
pr_numbers: [30]
status: open
transcript: /home/estoffer/.claude/projects/-home-estoffer-tinkle/9a0335e9-300c-4543-a435-c7fc11db6686.jsonl
---

# Session 4 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Persistence — NVS read/write, write-on-change, forward-compat schema (#25)

**Completed:**
- New `Persistence` module over an injected `IKeyValueStore` — `src/core/persistence.{h,cpp}` — mirroring the established core/shim/fake injection idiom (`IGpio`/`ArduinoGpio`/`FakeGpio`).
- `src/esp32/preferences_store.h` — Arduino `Preferences` (NVS) shim, the only NVS-touching code; single `tinkle` namespace.
- **DEC-008 forward-compat schema:** per-zone-indexed keys (`z<N>_dur`) read-with-default, iterated over the runtime `zoneCount` (capacity from `ValveConfig::MAX_ZONES`, never a second constant), + a `schema_ver` migration hook (additive changes don't bump it). Adding a zone post-V1 needs no migration — supersedes the Session 3 "lock around ZONE_COUNT=3" note (Eric: zones will be added later).
- Persists the three scalars that exist today: per-zone default durations (retires the `BUTTON_RUN_SEC=600` placeholder in `main.cpp` — manual button runs now read `persistence.zoneDefaultSec(z)`), `swMaxRuntimeSec`, cached diverter position. Write-on-change (set-to-current touches no flash). 15-char NVS key limit guarded at the source in `zoneKey()`.
- `main.cpp` — `prefsStore.begin()` + `persistence.begin()` in `setup()` after `runController.begin()`.
- 6 native tests + a write-counting `FakeKeyValueStore` (`test/test_native/test_main.cpp`): defaults-on-empty, schema-stamped-once, round-trip-across-reboot, write-on-change, diverter tri-state, zone bounds.
- Docs: DEC-008 (`DECISIONS.md`), §8 schema note (`tinkle_firmware_spec.md`).

**Verification:** `pio test -e native` 48/48 green (was 42); `pio run -e esp32` SUCCESS, Flash 21.0→21.7%. @architect blessed the scope cut + keying before implementation. PR #30 carries a tiered, human-runnable test plan (host tests → build → Wokwi smoke → a reboot-instrumentation recipe to *watch* NVS persist → bench boot-safety flag).

**Code review:** No bugs, no security issues. Three advisory cleanups, all deferred-wiring-by-design (DEC-008): `swMaxRuntimeSec` mirror + diverter cache are stored but not yet read back into the run path (RunController/fert config land later — latent dual-source for the 1200 s ceiling, both defaults agree for now); `PreferencesStore::begin()` discards the NVS-init bool (defaults are fail-dry safe, but a failed open is silent). Single-source-of-truth, schema-version order, key guard, write-on-change, C++11 — all verified clean.
**PR:** [#30](https://github.com/mobiustripper42/tinkle/pull/30) — closes #25
**Points:** 5
**Branch:** task/2.1-persistence
**Opened at:** 2026-06-06T03:28:36Z

**Next Steps:**

**Context:**
