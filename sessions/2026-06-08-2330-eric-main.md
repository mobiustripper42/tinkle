---
session: 7
dev: eric
slug: main
branch: main
started: 2026-06-08T23:30:06Z
ended:
points:
pr_numbers: [42]
status: open
transcript: /home/eric/.claude/projects/-home-eric-tinkle/7f811515-5690-4d4a-bf34-9d066059d228.jsonl
---

# Session 7 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: v1.4 valve re-architecture — on/off FETs, no master, two-leg diverter (task 1.8)

**Completed:**
- Diagnosed that the v1.4 commits (#40) were docs-only — the firmware was still the v1.3 H-bridge/latching/master model and contradicted the now-authoritative specs. Did the fix-forward rework (no rollback needed; no bad code had landed).
- `src/core/valve_driver.{h,cpp}` — rewritten to the on/off model: one low-side FET per valve (`openZone`/`closeZone`, hold-while-energized), diverter as two leg FETs (`setDiverter(fertigate)`), per-actuator travel timers; `tick()` clears only the busy flag (no FET coast). Removed master, H-bridges, `Bridge`, pulse API, never-both-high invariant. `safeState` = pump-off-first → de-energize every FET (diverter → plain).
- `src/core/run_controller.{h,cpp}` — dropped `OpenMaster`/`CloseMaster`; sequence `PrepDiverter→OpenZone→StartPump→Running→StopPump→CloseZone→Settle`. Watchdog pre-open gate at `OpenZone`. Diverter-skip keys on in-memory `diverterFert()` (boot state known-plain — no NVS cache).
- `src/core/persistence.{h,cpp}` — removed the `div_pos` cache (DEC-013).
- `src/esp32/pins.h` — re-mapped to the wiring doc §B (Z1/2/3_FET 13/14/16, DIV_CLEAN/FERT 17/18, no MASTER_FET; GPIO 21/12/15 freed). `src/esp32/main.cpp` — new valve config, dropped diverter restore/write-back. `src/attiny/main.cpp` — pump-only gating comment. `diagram.json` — dropped MASTER LED, added valve-FET + diverter-leg LEDs.
- `test/test_native/test_main.cpp` — valve/RC/persistence/fert tests rewritten; both-high policing removed; added `test_rc_first_plain_run_skips_diverter`. **72 tests, 0 failures.**
- Docs: renumbered task 1.7→1.8 in PROJECT_PLAN (1.7 was the button model, #23) + marked done; flipped DECISIONS + firmware-spec notes from pending to implemented.

**Verification:** PlatformIO isn't installed in this env (CLAUDE.md claims `~/.local/bin/pio` but it's absent), so `pio test -e native` / `pio run -e esp32` were NOT run by the real harness. Verified via g++ `-std=gnu++11` + a local Unity shim: 72/72 green; `src/core` compiles clean; all `main.cpp` symbol references line up with the new APIs. **Run `pio test -e native` + `pio run -e esp32` locally before merge.**

**Code review:** @code-review clean bill — no bugs. Verified fail-dry chain (pump-off sole gate, boot + every fault route through `safeState` pump-off-first, no path holds water on), watchdog gate at `OpenZone` entry before pump start, pin map vs wiring doc §B exact, diverter-skip sound without the cache, millis() rollover safety. Only findings were three stale master/both-high test comments — fixed in 962f902.
**PR:** [#42](https://github.com/mobiustripper42/tinkle/pull/42)
**Points:** 8
**Branch:** task/1.8-valve-rearchitecture
**Opened at:** 2026-06-09T13:15:31Z

**Next Steps:**
- Run `pio test -e native` + `pio run -e esp32` on a machine with PlatformIO to confirm the real harness agrees (the sandbox lacks pio).
- Housekeeping left from the v1.4 audit (separate from this rework): remove/archive `docs/DRAFT-v1.4-valve-rearchitecture.md` + `docs/DRAFT-v1.4-BOM.md`; optionally scrub the v1.3 changelog stragglers in `docs/tinkle_v1_spec.md` (lines ~15/28 — likely intentional dated changelog).
- Resume Phase 3: #35 Flow fault detection (3), then #36 Calibration (5) — now building on the v1.4 `ValveDriver`.

**Context:**
- ZONE_TRAVEL_MS = DIVERTER_TRAVEL_MS = 10000 (§15 seeds, bench-confirm in Phase 6). Test config uses short synthetic windows (75 ms zone / 6000 ms diverter) so the cooperative-loop tests run fast.
- v1.4 diverter rest state is plain *by construction* (both leg FETs de-energized via gate pulldowns), so boot position is known without NVS — this is why the cache was removable and why a first plain run skips travel.
- The wiring doc is the pin source of truth and was already v1.4; pins.h now matches it.

**[PAUSED 15:21 UTC]** Task 1.8 already shipped (PR #42) — nothing in flight on the task branch. Working tree was clean except a stray empty `package-lock.json` (6-line, `packages:{}`) that some tool dropped; removed it — this is a `tool` project with no `package.json`, it should never have one. Next on resume: (1) run the real `pio test -e native` + `pio run -e esp32` if on a box with PlatformIO (this env lacks it); (2) the v1.4 housekeeping leftovers (DRAFT-v1.4-*.md removal, tinkle_v1_spec.md changelog stragglers); or (3) start Phase 3 #35 Flow fault detection. Then `/its-dead` to close, or `/retro` if ending Phase 1.

**[RESUMED 00:10 UTC]**

**Context:**
