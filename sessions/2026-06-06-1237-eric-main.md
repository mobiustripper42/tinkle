---
session: 5
dev: eric
slug: main
branch: main
started: 2026-06-06T16:37:06Z
ended:
points:
pr_numbers: [31, 32, 33]
status: open
transcript: /home/estoffer/.claude/projects/-home-estoffer-tinkle/536d4f36-a337-4541-9919-20b83591152d.jsonl
---

# Session 5 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Clock — NTP sync + free-running millis() fallback, local-epoch seam (#26)

**Completed:**
- New platform-independent `Clock` over an injected `IWallClock` seam — `src/core/clock.{h,cpp}` — mirroring the Persistence/ValveDriver core·shim·fake idiom. Anchors a synced reading to a `millis()` instant and free-runs between reads; brisk 1 s retry until lock-on, then a 1 h re-anchor so free-run is exercised and NTP corrects drift.
- **DEC-009 local-epoch seam:** `IWallClock` hands core *local* epoch seconds, so timezone + DST stay in the ESP32 shim and core is a pure, host-testable epoch→fields derivation. `src/esp32/system_clock.h` (`SystemClock`) is the only SNTP/timezone code — `configTzTime` (US-Eastern POSIX TZ + `pool.ntp.org`/`time.nist.gov`), `localtime_r` re-packed via the pure `epochFromCivil` (ESP32 newlib lacks `tm_gmtoff`).
- Exposes `valid()` (→ display `clockValid`), `wall()` (HH:MM + weekday), `epoch()`, and a `minuteRolled()` per-minute eval edge for the Scheduler (#27).
- `main.cpp` — wired to the display, retiring the hardcoded `di.clockValid = false`. Idle shows HH:MM once NTP syncs, `--:--` until then. `systemClock.begin()` + `wallClock.begin()` in `setup()`; `wallClock.tick()` in the loop.
- 7 new native tests + a `FakeWallClock` in `test/test_native/test_main.cpp`: invalid-until-sync, field derivation, free-run between syncs, throttled-resync-then-correct, lock-on-when-source-appears, `epochFromCivil` round-trip (incl. leap day), `minuteRolled` edge.
- Docs: DEC-009 (`DECISIONS.md`), §13 note (`tinkle_firmware_spec.md`).

**Verification:** `pio test -e native` 55/55 green (was 48); `pio run -e esp32` SUCCESS. Host suite + both builds.

**Code review:** No blocking issues. Acted on two findings — hardened the `epochFromCivil` 2038 signed-`long` overflow (the 64-bit host had masked it) and added a leap-day round-trip test. Documented the `minuteRolled`/Scheduler idempotency contract instead of clamping the clock forward (clamping would defeat NTP drift correction). One advisory left as-is: the DST/TZ path in `SystemClock::localEpoch` is ESP32-only and can't run on the host — an accepted seam limitation.
**PR:** [#31](https://github.com/mobiustripper42/tinkle/pull/31) — closes #26
**Points:** 3
**Branch:** task/2.2-clock
**Opened at:** 2026-06-06T17:38:18Z

**Next Steps:**
- Phase 2 remaining: **#27 Scheduler (5)** — entry model (§13 `ScheduleEntry`), per-minute eval (consumes `Clock::minuteRolled()`), queue + overlap guard. Then #28 Fertigation policy (3).
- **#27 contract reminder (DEC-009):** the Scheduler must be idempotent per (entry, minute) — an hourly NTP resync can re-fire `minuteRolled()` around a boundary.

**Context:**
- Clock is NTP-only for V1 (tunnel has internet; manual time-set deferred as redundant). The `IWallClock` seam is ready for a DS3231 RTC if reboot-survival/offline ever matters.
- No battery RTC: clock is dark (`--:--`) until first NTP sync after each boot — and there's no WiFi until Phase 4, so the path stays invalid in sim/bench until then. Fail-dry safe (dark clock → no scheduled runs).

## Task 2: Scheduler — minute-keyed eval, fert policy, IRunSink seam (#27)

**Completed:**
- New in-memory `Scheduler` (`src/core/scheduler.{h,cpp}`) — `ScheduleEntry` model (§13), `add`/`clear`/`count`/`entry` editing surface, per-minute `tick()` + `evalNow()` for "on edit". `MAX_ENTRIES=16`. Evaluates due entries off the `Clock`, enqueues runs, applies the §6 fert policy.
- **DEC-010 `IRunSink` seam** (`src/core/run_controller.h`): callers that only *request* runs (Scheduler now, web API Phase 4) depend on a narrow `requestRun()` interface; `RunController` implements it. Matches the `IGpio`/`IKeyValueStore`/`IWallClock` idiom — scheduler logic host-tested against a `FakeRunSink` in isolation.
- **Overlap** stays the `RunController` queue's job; a due run that can't enqueue is dropped + counted (§13). **Fert policy (§6):** first `Auto` run of each calendar day fertigates, slot consumed only on successful enqueue; `On`/`Off` bypass the slot.
- **Idempotent eval**, forward-only (`curMin <= lastEvalMin_`): each absolute local minute evaluated at most once ever — drops the DEC-009 hourly-resync backward nudge even across a minute boundary.
- `main.cpp` — `scheduler.tick()` wired after the clock (no-op until populated / clock valid).
- 9 new native tests + `FakeRunSink`. Docs: DEC-010 (`DECISIONS.md`), §13 note (`tinkle_firmware_spec.md`).

**Verification:** `pio test -e native` 64/64 green (was 55); `pio run -e esp32` SUCCESS. Host suite + both builds.

**Code review:** @code-review was **unavailable** (repeated 529 backend overloads — two ~3.5 min hangs before I cut it). Reviewed **inline** instead, which caught a real bug: the idempotency guard (`curMin == lastEvalMin_`) only blocked the *current* minute, so a sub-second backward resync nudge across a boundary (06:00:00→05:59:59) would re-fire the previous minute's entry. Fixed to forward-only eval + added a backward-nudge test. Flagged for a human pass: the fert daily-slot logic and the `IRunSink` vtable addition to `RunController`.
**PR:** [#32](https://github.com/mobiustripper42/tinkle/pull/32) — closes #27
**Points:** 5
**Branch:** task/2.3-scheduler
**Opened at:** 2026-06-06T18:30:13Z

**Next Steps:**
- Phase 2 last task: **#28 Fertigation policy (3)** — one fert run/day + per-entry override. NOTE the §6 *policy* (first-auto-run-of-day, On/Off overrides) already landed here in the Scheduler; #28 is likely the remaining actuation/edge nuance (diverter sequencing already in RunController). Re-read #28 scope against what Scheduler already covers — may be smaller than 3 pts, or refocus on fert-specific run sequencing / status.
- After #28, Phase 2 closes → `/retro`.

**Context:**
- `evalNow()` ("on edit") re-evaluates the whole current minute, so it can re-fire entries already run this minute. Not wired yet (no editor until Phase 4); refine to per-entry dedup when the web API actually calls it.
- Schedule entries are RAM-only — NVS persistence deferred to the Phase 4 editor (DEC-010), consistent with DEC-008's "owning module persists when it lands."
- @code-review backend was overloaded (529s) this session — if it persists, review inline rather than burning wall-clock on retries.

## Task 3: Fertigation — wire cached diverter position + end-to-end tests (#28)

**Completed:**
- **Scope finding:** #28's acceptance criteria were already satisfied by #25–#27 — the §6 policy (one fert run/day, per-entry `auto|on|off` override, day boundary off the Clock) lives in `Scheduler` (#27); `RunController` already sets the diverter in `PrepDiverter` from `RunRequest::fertigate` with the skip-when-unchanged guard (#25); fail-dry holds (diverter travels before master opens; master FET gates water). Confirmed scope with Eric → wire the one unwired piece + lock the criteria with tests.
- **Cached diverter position survives reboot** (§6/§8, closes the deferred 2.1 @code-review flag): `ValveDriver::assumeDiverter()` seeds the known position from NVS at boot without driving the motor (motorized ball valve holds position with no power); `main.cpp` persists it on the **travel-complete** edge (`!diverterBusy()`) so NVS records the position actually reached.
- **End-to-end test** through the real stack (`Scheduler → RunController → ValveDriver`/FakeGpio): first auto run of the day routes THROUGH, next routes AROUND — first time the Scheduler is exercised against the real RunController + diverter, not a fake sink.
- **Diverter NVS round-trip test:** boot-seed skips the 6 s travel; persist-on-travel records position.
- Docs: §6 + §8 notes, DEC-008 update.

**Verification:** `pio test -e native` 66/66 green (was 64); `pio run -e esp32` SUCCESS. Host suite + both builds.

**Code review:** @code-review available this round (backend recovered). No blocking issues; confirmed `assumeDiverter` sound (no GPIO writes, correct boot-seed ordering) and tests prove their claims. Acted on its one finding: moved the persist poll from command-time to **travel-complete** (`setDiverter` sets the cached position at command time, so a power loss mid-travel would have recorded a destination the valve never reached). Residual mid-travel-power-loss edge (valve stranded mid-stroke, one possible mis-route, never holds water on) documented as accepted (§8).
**PR:** [#33](https://github.com/mobiustripper42/tinkle/pull/33) — closes #28
**Points:** 3
**Branch:** task/2.4-fertigation
**Opened at:** 2026-06-06T18:49:26Z

**Next Steps:**
- **Phase 2 complete** once #33 merges (all of #25–#28 shipped). Run **`/retro`** to close Phase 2 — computes per-session time math (Session 4 + Session 5), aggregates velocity, marks PROJECT_PLAN `[x]`, writes RETROSPECTIVES, then optionally `/start-phase` for Phase 3 (Flow + Calibration: #3.1 FlowMonitor, #3.2 fault detection, #3.3 calibration).
- Run `/its-dead` to close Session 5 — 3 tasks, 11 pts: Clock #26 (3) + Scheduler #27 (5) + Fertigation #28 (3).

**Context:**
- `swMaxRuntimeSec` is still stored-not-read-back (the *other* deferred 2.1 flag) — wire it from Persistence into RunController when the settings API lands (Phase 4). Both diverter-cache and swMax were the two 2.1 @code-review deferrals; diverter is now done, swMax remains.
- Fert *policy* lives in the Scheduler, fert *actuation* in RunController, fert *persistence* (diverter) wired via main's poll — three layers, one feature. Don't look for a standalone "fertigation module"; there isn't one by design.
