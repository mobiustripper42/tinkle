---
session: 10
dev: eric
slug: 5.6-sim-test
branch: task/5.6-sim-test
started: 2026-06-22T23:32:05Z
ended: 2026-06-24T18:33:27Z
points: 10
pr_numbers: [78, 79]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-tinkle/6745bfb3-9a79-4c38-a52e-9dc54d784c67.jsonl
---

# Session 10 — 5.6-sim-test

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 5.7 — phone-only pivot (DEC-019): drop buttons, TM1637 display, LED rings (#77)

**Project turn:** mid-session, eric pivoted Tinkle to **phone-only** — no buttons, no display, no on-box panel. The buttons are real fabrication + parts + enclosure-cutout work he can't afford against the deploy date; the SPA already does every panel job. A deletion pass, not a redesign. The cut firmware is git-recoverable if ever resurrected (zero current plans).

**Completed:**
- @architect review first (proceed-with-changes): safety chain provably untouched; key correction taken — **do not narrow SPEC's local-autonomy claim** (SoftAP adds no infra dependency; schedules still headless), instead retire DEC-006's narrower "enclosure-local override" sub-claim.
- Deleted `src/core/buttons.{h,cpp}`, `src/core/display.{h,cpp}`, `src/esp32/display_tm1637.h`, `ArduinoButtonInput`; dropped `robtillaart/TM1637_RT` from `platformio.ini`.
- `src/esp32/pins.h`: dropped TM/BTN/LED pins, added `ALIVE_LED=2` (DevKitC onboard LED — zero added BOM), reshaped `Zone` to `{fetPin, name}`; freed 8 GPIO banked for future zones.
- `src/esp32/main.cpp`: ripped out the button policy / display render / LED-ring render; replaced with a ~1 Hz `ALIVE_LED` blink. Manual run/stop/clear noted as SPA-only.
- `test/test_native/test_main.cpp`: removed the 14 button + display tests (123 → **109/109 green**); `esp32`/`esp32_sim`/`attiny85` all SUCCESS.
- Authored **DEC-019** (supersedes DEC-005/006; amends DEC-011/016) and propagated coherence across 9 docs: SPEC (operator interface + Not-V1 guardrail, autonomy bullet deliberately untouched), firmware spec (§1/2/3/4/10/11/12/14/15/16), wiring HTML pin source-of-truth, hardware spec (new v1.5 changelog + BOM/power/ops/GPIO budget), CLAUDE-context, README, PROJECT_PLAN (5.7 row + 1.5/1.6 + Cuttable notes), DRAFT BOM/valve docs.
- `ALIVE_LED` is distinct from `HEARTBEAT_OUT` (GPIO4, ATtiny watchdog) — flagged everywhere to prevent conflation.

**Verification:** `pio test -e native` 109/109; `esp32` / `esp32_sim` / `attiny85` SUCCESS. Source sweep clean (no live deleted symbols).

**Code review:** Clean bill of health. Only finding: the Wokwi `diagram.json` still wires the deleted panel — deferred to Phase B (#62), not a blocker.
**PR:** [#78](https://github.com/mobiustripper42/tinkle/pull/78)
**Points:** 5
**Branch:** task/5.7-drop-buttons-display
**Opened at:** 2026-06-23T00:00:00Z

## Task 2: Phase 5.8 — SPA-driven sim e2e for phone-only (#62)

**Context:** the phone-only pivot (Task 1) deleted the buttons the old #64 sim scenarios used to trigger runs. The headless `wokwi-cli` runner can't reach the SPA (`[[net.forward]]` is VS Code-only) or serial (broken). eric chose **interactive-SPA-only** for the run path: drive runs from the phone UI in VS Code Wokwi, and keep a slim headless tier for the fail-dry checks that need no run.

**Completed:**
- **Salvaged from the closed #64** (both `#ifdef TINKLE_SIM`, confirmed absent from the production `esp32` build): `src/esp32/main.cpp` 15 Hz LEDC fake-flow source (GPIO19→GPIO27 via the FLOW switch); `src/esp32/wifi_manager.h` Wokwi-GUEST auto-join on empty NVS (stored creds still win — `begin()` reads ssid/pass into locals).
- `diagram.json` rebuilt: alive LED (GPIO2) + pump/zone/diverter LEDs + FLOW slide switch; removed the TM1637, btn1-3, led_z1-3 parts + connections. Valid JSON.
- `wokwi.toml`: phone-only notes + `[[net.forward]]` 8180→80 (interactive only).
- `test/sim/run.sh` salvaged (header de-buttoned); two new no-run scenarios — `01_boot_safe_state` (all actuators rest LOW at boot) and `02_idle_unexpected_flow` (unexpected idle flow energizes nothing — the fail-dry property; the latch is SPA-only, stated honestly). `README.md` + `MANUAL.md` (interactive SPA walkthrough, mill-dev VPS port-forward note) rewritten. `.gitignore` ignores `screenshots/`.

**Verification:** native 109/109; `esp32_sim`/`esp32`/`attiny85` green; `diagram.json` valid JSON; scenarios valid YAML; `run.sh` valid bash. Cloud sim NOT run here (no `WOKWI_CLI_TOKEN`; runs in Wokwi's cloud) — execution is eric's (interactive in VS Code + `run.sh` with a token).

**Code review:** Clean bill of health — `diagram.json` matches `pins.h` row-for-row, FLOW switch polarity + boot-quiet verified, `expect-pin` rest values match `valve_driver.cpp`, `TINKLE_SIM` fencing correct, Wokwi-GUEST preserves stored-creds-win. Applied the one actionable cleanup (63aba23): note that E2 latches at the **2nd** 5 s window boundary so the `delay` isn't tightened under threshold.
**PR:** [#79](https://github.com/mobiustripper42/tinkle/pull/79)
**Points:** 5
**Branch:** task/5.8-spa-sim-e2e
**Opened at:** 2026-06-23T01:00:00Z

**Next Steps:**
- Merge #78 (Task 1) and #79 (Task 2). Then eric runs the sim: interactive SPA session in VS Code (`test/sim/MANUAL.md`) + `test/sim/run.sh` with a `WOKWI_CLI_TOKEN`. **#62 closes when the interactive session passes** — it's referenced, not auto-closed.
- After #62: everything left (#51 §17 bench, Phase 6 wet) is parts-gated. `/retro` once #62 closes — Phases 4 + 5 (incl. the phone-only pivot) wrap in this window.

**Context:**
- **#64 is CLOSED** (superseded by the pivot), not merged. Its still-valid sim enablement was salvaged into Task 2 (#79). The branch `task/5.6-sim-test` this session was named after is retired; work moved to `task/5.7` then `task/5.8`, both off `main`.
- Phone-only sim has **two modes**: interactive VS Code SPA (run path, manual) + headless `wokwi-cli` (boot-safe + idle-flow only, automatable). Headless can't trigger a run (no buttons, no SPA reach) — by design, eric's choice.
- The deleted panel modules are recoverable from git history — DEC-019 is "deferred, not deleted." Root `CLAUDE.md` is the seeds shell; project facts live in `.claude/CLAUDE-context.md`.
- Heartbeat encoding caveat (carried): if travel/settle ever shrink so the inter-run gap < HB_TIMEOUT_MS, queued runs share one armed period and could trip HARD_MAX mid-queue.
