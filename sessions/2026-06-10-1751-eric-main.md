---
session: 9
dev: eric
slug: main
branch: main
started: 2026-06-10T17:51:42Z
ended:
points:
pr_numbers: [53, 54, 60]
status: open
transcript: /home/eric/.claude/projects/-home-eric-tinkle/0d9d65a8-8e4e-4d37-8703-6ac1ad2998f2.jsonl
---

# Session 9 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 5 Unit B — watchdog handshake (5.1+5.2) + FaultManager (5.3 software) (#48/#49/#50)

**Completed:**
- DEC-016 committed (Phase 5 before Phase 4, batch units B/C/D) after @architect approval; Phase 5 materialized as issues #48–#52 (5.5 = DEC-014 self-test added per the architect's carve-out; 5.4 `blocked`/parts-gated).
- `src/core/watchdog_trip.h` — ATtiny trip logic (Disarmed/Armed/Lockout), header-only, compiled into BOTH `attiny85` and `native` (DEC-016 condition). Encoding: heartbeat present == pump commanded; silent disarm on quiet (clean run end ≠ trip); TRIPPED only on the HARD_MAX lockout, self-releasing after heartbeat-quiet — that released line is the §14 resolved signal.
- `src/core/watchdog.{h,cpp}` — ESP32 half: heartbeat only during `StartPump`/`Running` (DEC-003; inter-run gap ≥ ~21 s ≫ 2 s timeout ⇒ per-run ceiling), trip verdict routed via `raiseFault` (verdict-not-actuation).
- `src/core/fault_manager.{h,cpp}` — fault-log ring (8) + resolved-condition clear gate; all clear paths through `requestClear()`.
- `src/attiny/main.cpp` — real sketch: PB2 heartbeat in, PB1 ARM (low first thing on reset), PB0 TRIPPED active-low open-drain emulated (5 V-safe, absent watchdog reads not-tripped).
- `src/esp32/main.cpp` — loop wiring; long-press clear via FaultManager; `TINKLE_SIM` forces the trip read clear (Wokwi floats GPIO36 low).
- `test/test_native/test_main.cpp` — 14 new tests (105 total) + static_assert tying HEARTBEAT_MS ≤ HB_TIMEOUT_MS/4.
- `docs/tinkle_firmware_spec.md` — §9/§14 implemented-notes documenting the protocol decisions.

**Verification:** `pio test -e native` 105/105; `esp32`, `esp32_sim`, `attiny85` all SUCCESS (ATtiny 954 B / 8 KB).

**Code review:** 1 real bug found and fixed (46e6ba4): UnexpectedFlow resolved-condition qualified with `isIdle()` defeated itself once latched (state=Fault ⇒ reads resolved while water still pulses) — now unqualified. Plus hardening: watchdog state re-read post-flow-verdict, `Fault::Count` sentinel, protocol static_assert, clearFault() ownership doc.
**PR:** [#53](https://github.com/mobiustripper42/tinkle/pull/53)
**Points:** 16
**Branch:** task/5.1-watchdog-unit-b
**Opened at:** 2026-06-10T18:29:58Z

## Task 2: Phase 5.5 — DEC-014 auto-return self-test: ValveRestMonitor, non-latching flag (#52)

**Completed:**
- `src/core/valve_rest_monitor.{h,cpp}` — post-close rest window (SETTLE entry → IDLE, `REST_WINDOW_MS` 10 s seed / 3 s sim): > `REST_MAX_PULSES` (5 seed) flags the just-closed zone; flag self-heals on a clean window; chained queued run aborts the check (last run of a queue session still checked).
- `src/core/fault_manager.{h,cpp}` — `note()`: non-latching log-ring entry; `append()` refactor.
- `src/core/run_controller.{h,cpp}` — `Fault::ValveRest` (log-only, E6) + **structural firewall**: `raiseFault(ValveRest)` is a hard no-op, plus a `>= Count` guard.
- `src/esp32/main.cpp` — wiring after the fault block (fresh state read: a same-pass fault aborts the window, never gets measured by it); flag → `note()` + serial.
- `test/test_native/test_main.cpp` — 7 new tests (112 total).
- `docs/tinkle_firmware_spec.md` — §14 implemented-note; §15 `REST_WINDOW_MS`/`REST_MAX_PULSES` seed rows.

**Verification:** `pio test -e native` 112/112; `esp32`, `esp32_sim`, `attiny85` all SUCCESS.

**Code review:** Clean — both hardening suggestions applied (f3083ed): structural firewall in `raiseFault`, fault-aborts-window test.
**PR:** [#54](https://github.com/mobiustripper42/tinkle/pull/54)
**Points:** 3
**Branch:** task/5.5-auto-return-self-test
**Opened at:** 2026-06-10T21:49:29Z

## Task 3: Phase 4 Unit C — web API: core Api policy, async server, WiFi/mDNS, DEC-015 override (#55/#56/#57)

**Completed:**
- Phase 4 materialized first (issues #55–#59; new row 4.x = DEC-015, phase total 24 → 27).
- `src/core/api.{h,cpp}` — host-tested §10 endpoint policy (ArduinoJson on native: tests assert real wire shapes). FAULT gate with `/stop`+`/fault/clear`+`/settings` exemptions (the last is the documented DEC-015 deviation); atomic schedule replace → NVS (packed 10-byte entries) → `evalNow`; clear-on-enable through the sanctioned gate; validation seeds: durations 10–7200 s, swMax 60–1800 s (≤ ATtiny ceiling), K 50–5000.
- `src/esp32/web_server.h` — async routes + body collection (4 KB cap, defensive bounds) + ONE FreeRTOS mutex serializing handlers (other core) against the loop; 503 on 250 ms lock timeout. NVS worst-case-commit bound documented.
- `src/esp32/wifi_manager.h` — non-blocking STA join (NVS creds, 20 s) → open SoftAP `Tinkle-Setup`; mDNS `tinkle.local`; creds apply next boot.
- DEC-015: `FlowFaultDetector::setMuted` (verdicts quiet, tracking continues), `flow_ovr` NVS flag, status surface, watchdog untouchable.
- Persistence: `IKeyValueStore` grows str/bytes; WiFi creds + schedule blob + override; boot rehydration in main; `swMaxRuntimeSec` applies live.
- 11 new native tests (123 total); spec §7/§10 implemented-notes.

**Verification:** `pio test -e native` 123/123; `esp32` (66.6% flash), `esp32_sim`, `attiny85` all SUCCESS.

**Code review:** 8 findings, all applied (ae8d22b). Real ones: `wifiManager.tick` outside the lock (shared state both directions); silently-dropped non-bool `flowOverride` + coerced non-string `wifi.pass` (both 400 + tests). Note: @code-review hit two API-529 outages before the third attempt succeeded.
**PR:** [#60](https://github.com/mobiustripper42/tinkle/pull/60)
**Points:** 16
**Branch:** task/4.1-web-api-unit-c
**Opened at:** 2026-06-11T00:36:29Z

**Next Steps:**
- Unit D (4.3+4.4, #58/#59, ~11 pts): SPA 6 screens (vanilla, mobile-first) + gzip→PROGMEM pipeline (< 50 KB; flash headroom is 33%). Calibration screen guidance: any zone into a measured container, Zone 3 preferred when plumbed. DEC-015 banner reads `flow.overrideActive`.
- Eric defers all sim verification to one simulated-bench session: WiFi join/SoftAP, `curl tinkle.local/api/status`, schedule-survives-reboot, §17 items.
- Bench notes for #51 (carried): ATtiny at 3.3 V; pulldown on relay driver; trip line active-LOW; tune `REST_WINDOW_MS`/`REST_MAX_PULSES`.

**Context:**
- The UnexpectedFlow clear-gate condition must stay UNQUALIFIED by run state (main.cpp comment explains; the wiring-expression bug class isn't natively testable — main.cpp doesn't compile on host).
- `FaultManager::requestClear()` is the only sanctioned `clearFault()` caller — Phase 4's `/api/fault/clear` must route through it.
- Heartbeat encoding is per-run by construction; if anyone ever shrinks travel/settle so the inter-run gap < HB_TIMEOUT_MS, queued runs would share one armed period and could trip HARD_MAX mid-queue.
