---
session: 12
dev: eric
slug: main
branch: main
started: 2026-06-26T04:15:43Z
ended:
points:
pr_numbers: [85, 87, 88, 89, 91, 102, 103]
status: open
transcript: /home/eric/.claude/projects/-home-eric-tinkle/a5eee24d-bb33-4f9b-be40-6491bcfcfa6d.jsonl
---

# Session 12 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 4.6 — RunLog core, persisted run-history ring (#69)

**Completed:**
- New `src/core/run_log.{h,cpp}` — dependency-free 32-deep ring of packed 11-byte records (`startEpoch u32 · zone u8 · durationSec u16 · centigallons u16 · flags u8 · faultCode u8`), little-endian pack/unpack mirroring `packScheduleEntry`, plus serialize/deserialize for the NVS blob (oldest-first so the head lands newest; torn/oversized-blob tolerant).
- `src/core/run_controller.{h,cpp}` — retired `RunSummary`; `lastRun()` is now the ring head (one source of truth). Pushes a `RunEntry` at SETTLE and on `raiseFault`. Volume + start epoch are pushed in by main via `noteRunVolume`/`noteRunStart`, preserving the FlowMonitor + Clock decoupling (same idiom as `raiseFault`). Pre-2025 epoch-sanity guard clears `clockWasValid` (`RUNLOG_MIN_VALID_EPOCH`).
- `src/core/persistence.{h,cpp}` — additive `runlog` NVS blob mirroring `sched` (`saveRunLog`/`loadRunLog`), no `schema_ver` bump (DEC-008).
- `src/esp32/main.cpp` — boot rehydrate (`loadRunLog → runLogRef`), start-epoch + live-volume pushes, falling-edge final-tally capture, debounced write-on-change via a dirty flag.
- `src/core/api.cpp` — `/api/status` `lastRun` reads the head; JSON shape unchanged (history fields deferred to #70).
- `test/test_native/test_main.cpp` — 8 new host tests. `pio test -e native`: 117 pass. `pio run -e esp32` + `-e esp32_sim`: both link clean (Flash 67.6%).

**Code review:** No blockers. Applied the one should-fix (capture the completed-run volume on the RUNNING falling edge so the persisted value matches the printed gallons); tightened the debounce comment. Verified little-endian round-trip, head-lands-newest serialize, boot rehydrate doesn't dirty the ring, `lastRun()==head`, decoupling, and the epoch guard.
**PR:** [#85](https://github.com/mobiustripper42/tinkle/pull/85)
**Points:** 5
**Branch:** task/4.6-runlog-core
**Opened at:** 2026-06-26T11:59:06Z

## Task 2: Phase 4.7 — GET /api/history endpoint (#70)

**Completed:**
- `src/core/api.{h,cpp}` — new `Api::getHistory(out, nowMs)`: serializes the RunLog run ring newest-first (`startEpoch`, `clockWasValid`, `zone`, `durationSec`, `gallons` = centigallons/100, `fertigate`, `result`, `fault`) + the FaultManager ring as-is (`code`/`atMs`, same shape as `/api/status`) + top-level `clockValid` and `uptimeMs`. Read-only, returns 200 always — no FAULT gate, no validation (§10 gating is mutating-paths-only). `uptimeMs` emitted in core (not the glue) so the shape is host-tested and dates the millis-domain fault ring via `ago(uptimeMs − atMs)`.
- `src/esp32/web_server.h` — `History` added to the `Get` enum; `/api/history` route registered, served under the existing loop mutex.
- Code review dedupe: promoted the `RunResult`→wire-name map to a shared `Api::resultName` (mirrors `faultName`/`stateName`), now called by both `getHistory` and the `getStatus` lastRun block (retired the duplicated inline switch).
- `docs/DECISIONS.md` + `docs/tinkle_firmware_spec.md` — corrected the history payload estimate (~3 KB → ~6 KB worst case at full depth).
- `test/test_native/test_main.cpp` — 3 new host tests. `pio test -e native`: 120 pass. `pio run -e esp32` + `-e esp32_sim`: link clean.

**Code review:** No bugs — confirmed genuinely read-only (const views only) + mutex/ArduinoJson discipline. Applied the should-fix (deduped `RunResult` stringify) and both nits (uptimeMs cross-ref comment, payload-estimate doc fix). Known `clockWasValid:false ⇒ startEpoch:0` is a #71 rendering concern, not a blocker.
**PR:** [#87](https://github.com/mobiustripper42/tinkle/pull/87)
**Points:** 3
**Branch:** task/4.7-history-endpoint
**Opened at:** 2026-06-26T15:42:29Z

## Task 3: Phase 4.8 — SPA History screen, 7th tab (#71)

**Completed:**
- `web/index.html` — new **History** tab (7th `<nav>` button + `#scr-history`): a Recent-runs card (newest-first — when · zone · MM:SS · gallons · fert/plain · result), a Recent-faults card, and a Refresh button. `loadHistory()` lazy-fetches `GET /api/history` on tab-open (via the `show()` hook) + Refresh; **not** in `renderAll()`/`poll()`, so it stays off the status poll.
- Run "when" = device-local wall-clock from `startEpoch` (formatted via `getUTC*` since `startEpoch` is local epoch) when `clockWasValid`, else `unsynced` (the #69 11-byte record has no millis stamp for pre-NTP runs); faulted rows show `⚠ faulted (E#)` (via a `faultCode` map over `FAULT_TEXT`) and tint the timestamp red. Fault rows reuse the existing `ago(uptimeMs − atMs)` render.
- Graceful degrade keeps last-known `H` on fetch failure (global DISCONNECTED banner fires via `api()`); mock `/api/history` added so `?mock=1` works offline.
- Verified: `node --check` clean; render helpers smoke-tested in node; `pio run -e esp32` + `-e esp32_sim` build; SPA bundle **10.6 KB gzipped** (<50 KB gate).

**Code review:** Clean — `@code-review` returned nits only ("ship it"); confirmed off-poll, lazy-fetch wiring, local-epoch reasoning, degrade, and mock fidelity. Nits left as-is with reasons (unescaped `innerHTML` is enum-only + matches `renderFaults`; faults-card raw codes mirror the Faults screen; `H.clockValid` is an unused contract field). `@ui-reviewer` not available in this env — **375px / 7-tab visual is unconfirmed** (mock preview: `http://mill-dev:8000/index.html?mock=1`).
**PR:** [#88](https://github.com/mobiustripper42/tinkle/pull/88)
**Points:** 3
**Branch:** task/4.8-history-screen
**Opened at:** 2026-06-26T16:49:23Z

## Task 4: SPA config-screen UX — STOP-ALL hide, unsaved-changes guard, valve-alert relabel (#81 #82 #83)

**Completed:**
- `web/index.html` — bundled three SPA UX issues in one PR:
  - **#81:** `show()` hides `#stopall` on Schedule/Settings, restores it everywhere else.
  - **#82:** a `.dirtybar` (● Unsaved changes + **Discard**) on both config screens; a `schedSetDirty`/`setSetDirty` choke-point all edit sites route through (indicator can't drift from the flag); a `requestShow()` nav guard that `confirm`s on leaving a dirty screen (Cancel stays, OK reverts). **Discard + OK-leave revert in place from a cached copy** (`schedSaved` for the schedule, the existing `SET` for settings) — no fetch, so it's offline-safe.
  - **#83:** "Maintenance flags" → "Valve service alerts" + a one-line hint.
- Verified: `node --check` clean; `pio run -e esp32` + `-e esp32_sim` build; bundle **11.5 KB gzipped** (<50 KB gate).

**Code review:** Dirty state machine confirmed airtight + STOP-ALL toggle correct at boot/restore. Applied the one should-fix (offline reentry-fetch could resurface "discarded" edits → now reverts in place from cache; also kills the silent-offline-Discard nit). Emergency-friction note (reaching STOP from a dirty config screen passes a confirm) is within #81 scope + §10.1 (SPA has no safety role).
**PR:** [#89](https://github.com/mobiustripper42/tinkle/pull/89)
**Points:** 4 (1+2+1)
**Branch:** task/spa-config-ux-81-83
**Opened at:** 2026-06-26T19:18:49Z

## Task 5: Correct §8 fault-log persistence claim — RAM-only by decision (#72)

**Completed:**
- Resolved #72 via Option 2 (the honest doc fix — the fault log was never NVS-persisted):
  - `docs/tinkle_firmware_spec.md` §8 — pulled the fault log out of the "Survives reboot and power loss" list; added a "Not persisted — RAM-only by decision (#72)" note (`FaultManager` ring, `LOG_SIZE=8`, `{code, atMs}` millis-domain, reset every boot, live via `/api/status` §14 + `/api/history` DEC-018).
  - `docs/DECISIONS.md` (DEC-018 scope guard) + `docs/PROJECT_PLAN.md` (separate-bug note) — open-choice → **Resolved (#72)**, feature pointed at #90.
  - `src/core/persistence.h` — fixed the §8-state comment that still listed the fault-log ring as NVS-bound (code review caught the same drift in source).
- **Filed #90** — "Fault log: epoch-stamped NVS persistence (deferred Option-1)", scoped ~4–5 pt (the millis→epoch timestamp-model change + ripple to api/SPA is the real cost).

**Code review:** Every claim verified against `fault_manager.{h,cpp}`/`api.cpp`; three docs internally consistent. Applied both findings (stale `persistence.h` comment; over-broad DEC-018 citation on `/api/status`). `pio test -e native` 120 pass (comment-only header touch).
**PR:** [#91](https://github.com/mobiustripper42/tinkle/pull/91)
**Points:** 2
**Branch:** task/72-faultlog-doc-drift
**Opened at:** 2026-06-26T22:28:01Z

## Task 6: Fault log — epoch-stamped NVS persistence (#90)

**Completed:**
- Implemented the deferred #90 feature (the persistence half of #72). `FaultManager::Entry` → `{code, epoch, clockWasValid}` (was millis `atMs`); `LOG_SIZE` 8→16; `tick()`/`note()` take `(epoch, clockValid)` pushed in from main (runlog idiom; pre-2025 sanity guard in `append()`); added `serialize`/`deserialize` (6 B packed, oldest-first) + a dirty flag.
- `persistence.{h,cpp}` — additive `faultlog` NVS blob (`saveFaultLog`/`loadFaultLog`), mirrors `runlog`, no `schema_ver` bump. `main.cpp` — boot rehydrate + **immediate** write-on-dirty. `api.cpp` — fault entries `{code, epoch, clockWasValid}` on both endpoints. `web/index.html` — one shared `fmtWhen(epoch, valid)` for run + fault rows.
- Docs flipped RAM-only/deferred → shipped (§8, DEC-018 scope guard, persistence.h comment, PROJECT_PLAN) — the feature made those claims false.
- `pio test -e native` 121 pass (new `test_fm_epoch_guard_and_persist`); esp32 + esp32_sim link clean; SPA bundle 11.5 KB gz.

**Code review:** Clean port of the RunLog pattern (serialize/guard/dirty all verified). **Applied the one should-fix:** fault log persists **immediately**, not on the run-log's 2 s debounce — a debounce would swallow the faults most correlated with the resets the persistence exists to survive. Bench bonus: a reboot no longer wipes fault history.
**PR:** [#102](https://github.com/mobiustripper42/tinkle/pull/102)
**Points:** 5
**Branch:** task/90-faultlog-nvs-persist
**Opened at:** 2026-06-27T04:01:19Z

## Task 7: Guard inter-run heartbeat gap vs HB_TIMEOUT_MS (#101)

**Completed:**
- `src/esp32/main.cpp` — file-scope `static_assert(2*zoneTravelMs + settleMs > hbTimeoutMs)` (+ `watchdog_trip.h` include). Between queued runs the heartbeat is absent across CloseZone+Settle+OpenZone; that gap must clear the ATtiny's de-arm timeout or a queue chains into one armed window and can trip HARD_MAX mid-queue. 6.2 tunes the travel constants — the fence stops a tune from eroding the gap.
- Strict `>` (not a 2× margin) so TINKLE_SIM's shortened 1 s travels (gap 3000 > 2000) still build — caught a margin-too-aggressive first cut that would've failed the sim build.
- `src/attiny/main.cpp` — note the cross-MCU coupling (the esp32 assert checks the ATtiny's default `hbTimeoutMs`); `docs/tinkle_firmware_spec.md` §9 — invariant now fenced.

**Code review:** Verified correct on every axis (emit window, gap-minimum derivation as a true lower bound, de-arm/reset semantics, both builds, no false positive/overflow). Took the one cleanup (the cross-MCU coupling note). esp32 + esp32_sim link clean; no native impact.
**PR:** [#103](https://github.com/mobiustripper42/tinkle/pull/103)
**Points:** 2
**Branch:** task/101-heartbeat-gap-guard
**Opened at:** 2026-06-27T04:14:55Z

**Next Steps:**
- **Phase 4 is fully closed once #91 merges** (#72 was the last open Phase 4 issue) → run `/retro` to close the phase.
- Open PRs to merge: #91 (#72 doc fix). Earlier tasks (#85/#87/#88/#89) + #86 are merged.
- Backlog for a future phase: **#90** (fault-log epoch-stamped persistence, ~4–5 pt). Two unrelated "Tiller" PRs (#80/#65) remain open — separate track.
- **Visually confirm the SPA at 375px in a browser** (still headless-unconfirmed across this session): History screen 7-tab density + the dirtybar/STOP-ALL-hide behavior + "Valve service alerts" label. Mock preview: `http://mill-dev:8000/index.html?mock=1`.

**Context:**
- `clockWasValid:false` run rows render `unsynced` (no relative time) — a known limit of the #69 record (stores `startEpoch` only, no millis stamp). Fault rows still get true relative times (they carry `atMs`).
- `/api/history` payload worst case ≈ 6 KB at full depth — DEC-018 + firmware spec §10 estimates corrected from ≈3 KB in #87.
- **Clock drift (answered this session):** real ESP32 `millis()` is crystal-backed (~±20–50 ppm → ~2–4 s/day free-run), so the hourly NTP resync is comfortably sufficient; the sim's visible drift is a simulation artifact (simulated `millis()` lags real time), not a hardware prediction.
- Config-screen Discard reverts from a **cached** copy, not a fresh fetch — correct for a single-operator device; the lazy reload-on-tab-open still re-fetches when not dirty.
