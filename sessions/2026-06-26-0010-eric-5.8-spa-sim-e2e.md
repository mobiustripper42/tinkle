---
session: 11
dev: eric
slug: 5.8-spa-sim-e2e
branch: task/5.8-spa-sim-e2e
started: 2026-06-26T00:10:07Z
ended:
points:
pr_numbers: [79]
status: open
transcript: /home/eric/.claude/projects/-home-eric-tinkle/95edc009-90b6-4aff-aa89-7d57aa11d174.jsonl
---

# Session 11 — 5.8-spa-sim-e2e

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Private Wokwi Gateway — stable SPA connection for the interactive sim (#62)

**Completed:**
- Diagnosed the recurring ~16–25 s SPA drop in the VS Code Wokwi sim: the `net.forward` proxy opens a fresh socket per SPA poll and the device's small TCP pool exhausts (WiFi/sim stay up). The earlier slow-poll mitigation (`build_spa.py` 5s/8s for TINKLE_SIM, salvaged from the closed-not-merged #64) was re-applied then **reverted** — sim is back to production 1 Hz everywhere.
- Adopted the **paid Private Wokwi Gateway** as the real fix. `wokwi.toml` gets `[net] gateway = "ws://localhost:9011"`; gateway binary at `~/wokwigw/wokwigw-linux` (wokwi/wokwigw v2.0.1, Linux x86). Run `~/wokwigw/wokwigw-linux --forward 9080:10.13.37.2:80`, restart the sim → ESP32 on `10.13.37.2`, SPA stable at `http://localhost:9080/` at 1 Hz.
- Verified end-to-end: gateway logged `Client connected`; `GET localhost:9080/` → HTTP 200 with the current sim build's ETag (`2f5e7779`).
- Wrote a plain-language turn-it-on walkthrough in `test/sim/MANUAL.md`; propagated the gateway path into the `wokwi.toml` header + `test/sim/README.md`. Phone path documented: `tailscale serve --bg --https=8443 127.0.0.1:9080` → `https://mill-dev.tail7e2bfd.ts.net:8443/` (Chrome, Tailscale on).
- Memory `wokwi-sim-headless-e2e` updated with the adopted gateway setup.

**Code review:** Clean — TOML valid (`[net]` before `[[net.forward]]` is required and correct), slow-poll revert confirmed clean. Applied the doc-drift cleanups (gateway path in `wokwi.toml` header + README; banner casing).
**PR:** [#79](https://github.com/mobiustripper42/tinkle/pull/79) (pre-existing for this branch — Task 1 commits `c4ee550` + `be91a7b` joined it)
**Points:** 2
**Branch:** task/5.8-spa-sim-e2e
**Opened at:** 2026-06-26T02:38:49Z

**Next Steps:**

**Context:**
