---
session: 3
dev: eric
slug: main
branch: main
started: 2026-06-05T02:43:32Z
ended: 2026-06-05T03:19:02Z
points: 5
pr_numbers: [29]
status: closed
transcript: /home/estoffer/.claude/projects/-home-estoffer-tinkle/e131f432-c019-49bb-9acf-e14638fe575c.jsonl
---

# Session 3 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: 3-zone button model + DEC-006/DEC-007 (#23)

**Completed:**
- Implemented the resolved #23 design: **3 buttons = 3 zones, no dedicated stop** (DEC-006). Idle press → start that zone; any press while a zone runs → stop (no switch/auto-start); ≥3 s long-press any button → `clearFault()` + a rollover-safe all-rings ack flash.
- `src/esp32/pins.h` — `Z3_IN1=15`/`Z3_IN2=12`; Zone 3 added to the data-driven `ZONES` table (BTN3/LED3 now Zone 3); boot-strap safety comments.
- `src/esp32/main.cpp` — new uniform per-zone button policy + `FAULT_ACK_MS` success-ack window; stop button removed; LED loop reworked (old separate LED3 drive dropped).
- `src/core/display.{h,cpp}` — dropped `stopLedMode`; `zoneLedMode(activeZone, zoneIndex, faulted)` blinks **all** rings on fault (no dedicated stop ring).
- `test/test_native/test_main.cpp` — `test_display_led_modes` updated for the 3-arg signature + Zone 3 / fault-override cases.
- Docs: **DEC-006** (button model) + **DEC-007** (Z3 strapping pins, GPIO12-brick constraint) in `DECISIONS.md`; firmware spec §3/§11/§12; `SPEC.md`; `CLAUDE.md`; `tinkle_v1_spec.md`; `tinkle_wiring.html` (Z3 rows, caption 19→21, §D boot note); `diagram.json` (B3→Zone3, Z3 ring LED on GPIO23).

**Verification:** `pio test -e native` 42/42 green; `pio run -e esp32` SUCCESS (Flash 21.0%). @architect blessed the Z3 pin pair (GPIO15/12) and the §12 feedback scope boundary *before* implementation.

**Code review:** Clean Bill of Health — branches mutually-exclusive/exhaustive, no double-actuation, long-press-clear vs same-hold pressEdge ordering correct, ack-window comparison `(int32_t)(faultAckUntilMs-now)>0` rollover-safe + correct at boot, pin/spec/wiring consistent. Non-defect note: the composed `loop()` button policy is esp32-only glue (host-untested by design, DEC-004) → verify on the §17 bench checklist.
**PR:** [#29](https://github.com/mobiustripper42/tinkle/pull/29) — closes #23
**Points:** 5
**Branch:** task/1.7-three-zone-model
**Opened at:** 2026-06-05T03:05:00Z

**Next Steps:**
- Merge **PR #29** (closes #23). The 3-zone model now precedes Phase 2 Persistence, so the NVS schema (#25) can lock around `ZONE_COUNT=3` without a later migration.
- **Phase 2 is materialized** (#25–#28, 16 pts). Suggested start: **#26 Clock** (zone-count-independent) or **#25 Persistence** (now unblocked by #23). Cut `task/2.x-…` + `/kill-this` per task.
- **Bench/§17 gap (carry to Phase 6):** the FAULT/running/idle button *composition* in `main.cpp loop()` is host-untested glue — exercise it on the Wokwi/bench tier (per @code-review). The Wokwi diagram now shows all 3 zones + B3=Zone3, so the sequence is watchable.
- **Strapping-pin watch:** Z3 on GPIO15/12 is boot-safe *only* via the DRV8871 internal input pulldowns (GPIO12 must sample LOW = 3.3V flash). Confirm clean boot on bench; never put a pull-up/probe on GPIO12 (DEC-007 / wiring §D).

**Context:**
- `clearFault()` clears unconditionally when faulted (returns false otherwise); the "unresolved-hold → visible no-op" feedback branch is intentionally deferred to Phase 3/5 (no FaultManager resolved-condition signal yet). Speced in §12 + DEC-006, gated in a code comment.
- `ValveConfig::MAX_ZONES` and `Buttons::MAX_BUTTONS` were already 3 (build-for-three), so the 3rd zone needed no struct resizing — only the `ZONES` table + pins.
- Wiring doc wins on pins (CLAUDE.md): the DEC-007 reasoning lives in `tinkle_wiring.html` §D so the next person doesn't brick boot via a GPIO12 pull-up.
