---
session: 22
dev: eric
slug: main
branch: main
started: 2026-07-16T03:18:27Z
ended:
points:
pr_numbers: [143, 145, 146, 148, 149]
status: open
transcript: /home/eric/.claude/projects/-home-eric-tinkle/33e19dd9-225f-4787-bbb0-efc419e00ec2.jsonl
---

# Session 22 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: E2 false-latch discriminator (#141) + phantom Faulted log guard (#138)

**Completed:**
- `src/core/flow_fault_detector.h` — idle-flow threshold 50 → 139 pulses/5 s window (~0.36 → ~1.0 GPM at K≈1670). Draindown tail no longer false-latches E2 past the #124 drain-gate cap; welded pump relay (~1.45 GPM ≈ 202) still trips. At-grade catchment (operator-confirmed). `#ifdef TINKLE_SIM` keeps 50 so the 15 Hz sim fake-flow can still provoke E2.
- `src/core/run_controller.cpp` — `raiseFault()` log guard adds `state_ != Idle`; an idle-time fault no longer pushes a phantom Faulted duplicate of the previous run (current_/pending* were stale). Fault still latches + still logs to FaultManager.
- `docs/tinkle_firmware_spec.md` §15 — IDLE_FLOW_FAULT_PULSES table row updated.
- `test/test_native/test_main.cpp` — +2 tests (idle fault logs no phantom; default-config threshold ignores draindown, traps welded relay). 137/137 native; esp32 + esp32_sim build.

**Code review:** 1 real bug fixed pre-PR — 139 was unreachable by the sim's 15 Hz source, blinding the headless E2 scenario; fixed with the TINKLE_SIM split. 1 pre-existing edge filed as #144 (CalRange finish() phantom against an unrelated run).
**PR:** [#145](https://github.com/mobiustripper42/tinkle/pull/145) — closes #141, #138
**Points:** 5
**Branch:** task/e2-idle-discriminator-138
**Opened at:** 2026-07-16T18:02:57Z

## Task 2: Distributed Watering M1 — Scheduler even-spread core (#142, DEC-024)

**Completed:**
- `src/core/scheduler.{h,cpp}` — `DistributedConfig`, `computeDistributedPlan()` (shared plan math: runs ≥7-min floor / ≤6 cap, per-run length, bookended cycle-start minutes; invalid below floor / over-subscribed / out of minute-of-day bounds). `evaluate()` routes to `evaluateDistributed()` only when the plan is valid; each cycle fires one run per zone back-to-back, fert on first N cycles (whole-cycle). Per-day cycle bitmask → evalNow re-entry can't double-fire. pack/unpack for NVS. `distributedActive()`. static_asserts (MAX_ZONES≤MAX_QUEUE, DIST_MAX_RUNS≤8).
- `src/core/persistence.{h,cpp}` — `dist` NVS key, load-with-default (absent ⇒ disabled), atomic save. Additive DEC-008, SCHEMA_VER unchanged.
- `src/esp32/main.cpp` — boot-load; loud log if enabled-but-invalid.
- `docs/tinkle_firmware_spec.md` §15 — DIST_ constants.
- Scheduler ctor gained zoneCount (default ValveConfig::MAX_ZONES).

**Code review:** blocker fixed pre-PR — enabled-but-invalid config used to water nothing silently; now falls through to the fixed schedule + loud boot log (`distributedActive()`). Plus input-bounds hardening (u16 overflow guard) + MAX_ZONES≤MAX_QUEUE static_assert.
**PR:** [#146](https://github.com/mobiustripper42/tinkle/pull/146) — part of #142 (M1); DEC-024 lands via #143
**Points:** 8
**Branch:** task/142-distributed-watering-m1
**Opened at:** 2026-07-16T19:31:40Z
**Tests:** 147/147 native; esp32 builds.

## Task 3: Nightly idle-gated self-reboot + field disconnect diagnosis (#147)

**Completed:**
- `src/core/nightly_reboot.h` (new) — `NightlyReboot::due()` pure decision: once/local-day, post-midnight 60-min window, only when safely idle (isIdle + queue empty + !faulted + !ota + !calibration.active). Busy all window ⇒ skip; 2h min-uptime guard prevents reboot loop.
- `src/esp32/main.cpp` — wired into loop() INSIDE the webServer lock (concurrency fix from review); flush run/fault logs then ESP.restart().
- **Diagnosed the field disconnect** → filed #147: `WifiManager::tick()` (`wifi_manager.h:52-64`) does nothing once mode_==Sta — no dropped-STA recovery / mDNS re-announce beyond the SDK auto-reconnect. That's the likely root cause; nightly reboot is the mitigation. (P4 WiFi work was sim-only — Wokwi net.forward socket exhaustion — NOT this.)

**Code review:** block-level bug fixed pre-PR — reboot read RunController state outside the webServer lock (raced POST /api/run → could power-cycle a starting run). Moved inside the lock; added calibration.active() guard.
**PR:** [#148](https://github.com/mobiustripper42/tinkle/pull/148)
**Points:** 3
**Branch:** task/nightly-reboot
**Opened at:** 2026-07-16T19:44:50Z
**Tests:** 139/139 native; esp32 builds.

## Task 4: WiFi STA-drop recovery (#147) — the disconnect root-cause fix

**Completed:**
- `src/core/wifi_link_monitor.h` (new) — `WifiLinkMonitor::update()` pure/host-tested: sustained drop (>~8s, debounced) → Reconnect, retried every ~15s indefinitely; connected tick re-arms. No SoftAP fallback on a mid-season drop (self-heal onto the returning AP).
- `src/esp32/wifi_manager.h` — `tick()` now watches the Sta link (was a no-op once joined), calls `WiFi.reconnect()`, re-announces mDNS only after a real reconnect (gated via `reconnecting_`).
- `docs/tinkle_firmware_spec.md` §10 — recovery + no-fallback tradeoff documented.

**Code review:** no blockers; folded 2 cleanups (mDNS re-announce gated on real reconnect not raw up-edge; +millis() wraparound test).
**PR:** [#149](https://github.com/mobiustripper42/tinkle/pull/149) — closes #147
**Points:** 3
**Branch:** task/147-wifi-sta-recovery
**Opened at:** 2026-07-16T20:26:11Z
**Tests:** 139/139 native; esp32 builds.

**Decision:** #148 (nightly reboot) **parked as a draft backstop** (not merged) — #149 fixes the disconnect at the source; keep #148 one-click-away if the drop recurs (would imply a non-WiFi cause like heap).

**Next Steps:**
- **FLASH #149** for the disconnect fix (the immediate field priority). Field-verify: drop the AP briefly, box self-recovers in ~10–25s with no power-cycle.
- **Merge order (firmware):** #145 (E2 gate) → #143 (DEC-024 + @ui-reviewer) → #146 (DW M1). #149 independent — merge + flash anytime. To flash everything at once: merge the ready PRs to main, build main, upload.
- **Remaining from the user's list:** #142 M2 (SPA editor — fold in the mock), then #140 (UI-review fixes). Both still open.
- Strike DEC-024's "compose-later: volume dosing" line before #143 merges (#132 closed).
- #148 disposition: close if a week of #149 shows the disconnect gone; else merge as the nightly backstop.
- **Continue this session's build plan (user wants #142, #140 done too):** #142 M1 (DW firmware core) next, then M2 (SPA), then #140 (UI-review fixes). See mock: https://claude.ai/code/artifact/4ed76ad6-8afe-4757-b1b4-9c8559515410
- Flash from bee-grace after #145 merges; confirm at next wet run that block-end draindown no longer latches E2 and phantom History rows are gone. Instrument the draindown envelope to confirm the 139 value (#141 notes).
- Strike DEC-024's "compose-later: volume dosing" line before #143 merges (#132 closed as won't-do).

**Context:**
- Big triage + design session. Closed #99 (flow K verified), #136 (superseded by DW), #97/#98/#100 (Phase 6 bench, operator-verified — Phase 6 bench validation essentially done), #125 (replaced by DW), #132 (volume dosing rejected).
- Filed: #138, #139 (season target settings, deferred), #140 (UI review 6/10), #141, #142 (Distributed Watering / DEC-024), #144 (CalRange phantom).
- **Distributed Watering** (renamed from "pulse" per user) — DEC-024 written (in PR #143). Scheduler-level even-spread mode, either/or with schedule, one NVS record, whole-cycle fert (dropdown: fertigate first N waterings), 7-min floor/6-run cap, softened summer guideline (110–140, informational) + hard 25%-catchment block. Rotation is the soak — no RunController state.
- **@ui-reviewer wired** — `.claude/agents/ui-reviewer.md` was referenced but never existed; created (in PR #143). Registers next session.
- E2 diagnosis lesson: the "Zone 3 keeps faulting" signal was a structural artifact (Zone 3 is always last → only it precedes IDLE), not a stuck valve. Zone 1 basil line (4" tape) explains its elevated flow; the intermittent ~2× on Z1/Z2 is still an open question (unrelated to this PR).
- At-grade catchment confirmed (picks the simple E2 rate-bump fix over a duration discriminator).
