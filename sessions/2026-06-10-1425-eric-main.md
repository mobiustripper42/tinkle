---
session: 8
dev: eric
slug: main
branch: main
started: 2026-06-10T14:25:18Z
ended: 2026-06-10T17:07:17Z
points: 5
pr_numbers: [47]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-tinkle/2b672e08-ccd6-4345-b802-e83143edc134.jsonl
---

# Session 8 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Calibration mode — CalibrationController, K to NVS, sanity bounds (task 3.3, #36)

**Completed:**
- `src/core/calibration_controller.{h,cpp}` — new module: state machine (Idle/Running/Awaiting) + K math, host-testable. `start(zone)` requires both controller and RunController idle (a calibration never queues), requests a bounded run (`CAL_RUN_SEC` 120 s, `TINKLE_SIM` 10 s) with the diverter plain, baselines its **own** pulse tally (independent of FlowMonitor's per-run accumulation). Tally freezes at SETTLE entry — not IDLE, because a queued run chains SETTLE→next without visiting IDLE. `finish(measuredGallons)` → K = tally/gallons; on success writes NVS (`k_ppg`) + live FlowMonitor; rejection (volume < 0.25 gal or K outside [50, 5000]) raises `FAULT_CAL_RANGE` (§14 latch + safe state), K untouched. `cancel()` aborts without judging; a run fault voids the calibration.
- `src/esp32/main.cpp` — construct + per-tick wiring; start/finish/cancel wait for the Phase 4 endpoints.
- `test/test_native/test_main.cpp` — 10 new tests (91 total, green), incl. queued-run tally immunity and the write-on-change trap (test K must differ from the 450 default or the NVS write is correctly skipped).
- `docs/tinkle_firmware_spec.md` — §7 implemented-note; §15 `CAL_RUN_SEC` + cal sanity-bound seeds.

**Verification:** `pio test -e native` 91/91; `pio run -e esp32` SUCCESS.

**Code review:** @code-review skipped — eric is taking this review (per session agreement).
**PR:** [#47](https://github.com/mobiustripper42/tinkle/pull/47)
**Points:** 5
**Branch:** task/3.3-calibration
**Opened at:** 2026-06-10T14:43:00Z

**Next Steps:**
- `/retro` Phase 3 — #47 merged, #36 closed, phase is empty.
- @architect check on reordering Phase 5 before Phase 4 (finish the safety core, then the web layer — "firmware then app"). Agreed batch plan: **Unit B** = 5.1+5.2+5.3 (+5.x auto-return self-test) as one watchdog PR (~16–19 pts, both sides of one heartbeat protocol); **Unit C** = 4.1+4.2 (+DEC-015 override) web layer (~13–16); **Unit D** = 4.3+4.4 SPA + gzip pipeline (~11). 5.4 (§17 bench pass) stays parts-gated.
- Unit D's calibration screen: bake in the operator guidance — calibrate on Zone 3 (hose outlet) into a 5-gal bucket; flow meter is on the common line so K applies to all zones.

**Context:**
- Calibration tally freezes at SETTLE entry, not IDLE — a queued run chains SETTLE→next without visiting IDLE (run_controller.cpp:165).
- Persistence write-on-change means tests asserting an NVS write must use a K ≠ the 450 default, or the write is (correctly) skipped.
- Rejected calibration = latched FAULT_CAL_RANGE per §14, not a soft retry; the Phase 4 endpoint range-validates input before calling core.
