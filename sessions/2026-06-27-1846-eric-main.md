---
session: 13
dev: eric
slug: main
branch: main
started: 2026-06-27T22:46:49Z
ended: 2026-06-28T01:05:52Z
points: 2
pr_numbers: [106]
transcript: /home/estoffer/.claude/projects/-home-estoffer-tinkle/6b0dc4e7-0cf7-4ab9-82e0-667300ae4ca8.jsonl
status: closed
---

# Session 13 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Diverter returns to plain at SETTLE — DEC-021 (#105)

**Completed:**
- `src/core/run_controller.cpp` — SETTLE now returns the Dosatron diverter to plain rest (both leg FETs LOW) when no run is queued: `if (qCount_ == 0 && valve_.diverterFert()) valve_.setDiverter(false, nowMs)`. Was "left as-is" (no decision cited) — the fert leg rested open for up to ~24h between runs, violating the ratified rest-state contract (DEC-011/013, spec §5/§14/§17) for a skip-travel optimization that, under §6's one-fert-run/day policy, never fires. `qCount_==0` preserves the skip inside a queue (chained run sets its legs in PREP); `diverterFert()` keeps a plain run's SETTLE travel-free.
- `test/test_native/test_main.cpp` — rewrote `test_rc_diverter_skip_when_unchanged` (skip now proven inside a queue, not across separate runs) + 3 new: empty-queue return-to-plain, plain-run SETTLE no-op, and the residual-return-travel PREP wait (a run requested during the ~6s return-travel holds in PREP). **124 pass** (was 121).
- `src/core/valve_driver.h` — documented that `busy()`/`diverterBusy()` can read true at IDLE while the post-SETTLE return-travel timer runs down (FET levels already at rest; only the timer lingers, serializing a next run's PREP behind the return).
- `docs/DECISIONS.md` — **DEC-021**; `docs/tinkle_firmware_spec.md` §4 step 7 corrected ("left as-is" → queue-aware return).
- Surfaced during 6.1a bench validation; @architect-ratified before implementing (coherence defect vs SPEC, not a sanctioned optimization).

**Verification:** native `pio test -e native` **124/124**; `pio run -e esp32` + `-e esp32_sim` link clean; **flashed to the bench and confirmed live** — fert run lights 17/18, both de-energize at the end of the close sequence (after the ~10s zone-close travel, pump already off). No-flow override + pin-36 pull-up in place on the bench rig.

**Code review:** Clean bill — guard logic and test write-count assertions sound; contract held (pump off two states before the diverter touch; open fert leg dead-ended per DEC-013; rest monitor + heartbeat untouched). Applied both suggested cleanups (busy() doc note + residual-travel test) in this PR.
**PR:** [#106](https://github.com/mobiustripper42/tinkle/pull/106) — closes #105
**Points:** 2
**Branch:** task/diverter-rest-settle
**Opened at:** 2026-06-28T00:57:15Z

**Next Steps:**
- **Close 6.1a (#93).** All six outputs passed on the bench (zones 13/14/16 isolated, diverter 17/18, pump 22 sequenced). Two `docs/tinkle_buildup.md` gaps surfaced and want fixing before closing: (1) Part 1 assumes an onboard pin-2 alive LED this DevKitC lacks — needs an external LED on pin 2; (2) Part 1 never mentions the **pin-36 (VP) 10kΩ pull-up to 3.3V**, so a bench run trips **E3 (watchdog)** before Step 8 wires it. Both are real doc bugs from this session.
- Then Phase 6 continues: 6.1b (#94) 24V rail + 5V buck.

**Context:**
- Diverter LEDs read **FET drive state, not valve position** — 17 (NO bypass) and 18 (NC fert) are opposite-polarity valves, so fertigation drives **both** FETs HIGH (bypass closed + fert open). Plain run = both LOW.
- Bench board is a genuine DevKitC (not a clone) with **no GPIO LEDs populated** — every output, including the pin-2 alive heartbeat, needs an external LED. The bright steady green is the power LED (hardwired, not software-controllable).
- ESP32 is on **bee-grace** at `/dev/ttyUSB0` (CH340). Needed a data USB cable (first two were charge-only) + `estoffer` in `dialout` (chmod'd this session; permanent `usermod -aG dialout` still pending a re-login).
