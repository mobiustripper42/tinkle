---
session: 15
dev: eric
slug: 6.1b-24v-tvs
branch: task/6.1b-24v-tvs
started: 2026-06-30T01:02:05Z
ended:
points:
pr_numbers: [111]
status: abandoned
transcript: /home/estoffer/.claude/projects/-home-estoffer-tinkle/c5be4330-89f4-42d7-837d-bf20d227096c.jsonl
---

# Session 15 — 6.1b-24v-tvs

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: ATtiny watchdog — flash procedure doc + heartbeat pull-down fail-safe (6.1d, #96)

**Completed:**
- Flashed 3 ATtiny85 watchdogs (1 in service, 2 spares) using Arduino-as-ISP + avrdude — no IDE. Worked out the whole toolchain live (ArduinoISP onto the Uno, 10µF reset cap, lfuse 0xE2 = 8 MHz, `avrdude -c stk500v1 -P /dev/ttyACM0 -b 19200`). The `pio -t fuses`/`-t upload` targets don't pass the port to a stk500v1 programmer — avrdude direct is the verified path.
- `docs/tinkle_buildup.md` — new **Step 8.0** (the missing "flash the ATtiny" step; the guide wired the watchdog but never flashed it — a real doc gap). Step 8 wiring now pin-specific + the fail-safe pull-down + reset pull-up + ATtiny-local pull-up/cap option.
- **Fail-dangerous defect found + fixed at the bench:** disconnected heartbeat floated ATtiny pin 7 → could read as alive → pump stayed armed. Added `pin 7 → 10k → GND` pull-down; a broken heartbeat now reads quiet → disarm. Validated on the protoboard (pull pin 4 mid-run → arm drops ~2s).
- `docs/DRAFT-v1.4-BOM.md` — watchdog-support-passives row.
- `platformio.ini` — attiny85 env configured (board_fuses, Uno-as-ISP procedure documented).

**Code review:** Clean bill — pin map, ISP wiring, fuse bits, fail-safe logic all verified against firmware/BOM/ini. One nit (BOM double-count) fixed in follow-up.
**PR:** [#111](https://github.com/mobiustripper42/tinkle/pull/111) — does NOT close #96
**Points:** 3
**Blocked:** #96 physical acceptance (real safety relay gating the pump) waits on the relay part.
**Branch:** task/6.1d-attiny-flash-doc
**Opened at:** 2026-07-01T00:34:38Z

**Next Steps:**
- **#96 physical:** wire the real safety relay (`ATtiny pin 6 → relay coil`; `24V+ → relay → 24V-armed`; pump fed from 24V-armed). Waits on the relay module.
- Then Phase 6 continues: #97 pump + flow + fail-dry, #98 (set travel to **15s** — bench-confirmed), #99 flow cal, #100 wet confirm.
- Consider the single-board rebuild (#110) before the pump wiring gets added.

**Context:**
- This session also closed **#94** (24V/buck bench-metered) and **#95** (all 5 valve FET channels actuate from the SPA; diverter rests as plain water). #98 logged: valves fully open at **15s** (10s left them mostly-open).
- **Watchdog is a separate ATtiny85** — flash it (Step 8.0) before wiring; it gates only the pump, so valves run without it. Runtime power **3.3V** (not 5V) for clean heartbeat levels.
- The heartbeat pull-down (pin7→10k→GND) is safety-critical — without it a loose heartbeat wire fails dangerous.
- ATtiny flash rig: Uno on `/dev/ttyACM0` as ISP; hex at `.pio/build/attiny85/firmware.hex`. Filed **#110** (single-protoboard rebuild, ESP32-mounted).
